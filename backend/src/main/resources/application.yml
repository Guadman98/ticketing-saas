server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://192.168.186.36:5432/ticketing}
    username: ${DB_USER:ticketing}
    password: ${DB_PASS:ticketing}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
